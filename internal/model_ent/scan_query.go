// Code generated by ent, DO NOT EDIT.

package model_ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/ice-bergtech/dnh/src/internal/model_ent/asninfo"
	"github.com/ice-bergtech/dnh/src/internal/model_ent/dnsentry"
	"github.com/ice-bergtech/dnh/src/internal/model_ent/domain"
	"github.com/ice-bergtech/dnh/src/internal/model_ent/ipaddress"
	"github.com/ice-bergtech/dnh/src/internal/model_ent/nameserver"
	"github.com/ice-bergtech/dnh/src/internal/model_ent/path"
	"github.com/ice-bergtech/dnh/src/internal/model_ent/predicate"
	"github.com/ice-bergtech/dnh/src/internal/model_ent/registrar"
	"github.com/ice-bergtech/dnh/src/internal/model_ent/scan"
	"github.com/ice-bergtech/dnh/src/internal/model_ent/whois"
)

// ScanQuery is the builder for querying Scan entities.
type ScanQuery struct {
	config
	ctx            *QueryContext
	order          []scan.OrderOption
	inters         []Interceptor
	predicates     []predicate.Scan
	withIpaddress  *IPAddressQuery
	withAsninfo    *ASNInfoQuery
	withDnsentry   *DNSEntryQuery
	withDomain     *DomainQuery
	withPaths      *PathQuery
	withNameserver *NameserverQuery
	withRegistrar  *RegistrarQuery
	withWhois      *WhoisQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ScanQuery builder.
func (sq *ScanQuery) Where(ps ...predicate.Scan) *ScanQuery {
	sq.predicates = append(sq.predicates, ps...)
	return sq
}

// Limit the number of records to be returned by this query.
func (sq *ScanQuery) Limit(limit int) *ScanQuery {
	sq.ctx.Limit = &limit
	return sq
}

// Offset to start from.
func (sq *ScanQuery) Offset(offset int) *ScanQuery {
	sq.ctx.Offset = &offset
	return sq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (sq *ScanQuery) Unique(unique bool) *ScanQuery {
	sq.ctx.Unique = &unique
	return sq
}

// Order specifies how the records should be ordered.
func (sq *ScanQuery) Order(o ...scan.OrderOption) *ScanQuery {
	sq.order = append(sq.order, o...)
	return sq
}

// QueryIpaddress chains the current query on the "ipaddress" edge.
func (sq *ScanQuery) QueryIpaddress() *IPAddressQuery {
	query := (&IPAddressClient{config: sq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := sq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := sq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(scan.Table, scan.FieldID, selector),
			sqlgraph.To(ipaddress.Table, ipaddress.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, scan.IpaddressTable, scan.IpaddressColumn),
		)
		fromU = sqlgraph.SetNeighbors(sq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryAsninfo chains the current query on the "asninfo" edge.
func (sq *ScanQuery) QueryAsninfo() *ASNInfoQuery {
	query := (&ASNInfoClient{config: sq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := sq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := sq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(scan.Table, scan.FieldID, selector),
			sqlgraph.To(asninfo.Table, asninfo.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, scan.AsninfoTable, scan.AsninfoColumn),
		)
		fromU = sqlgraph.SetNeighbors(sq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryDnsentry chains the current query on the "dnsentry" edge.
func (sq *ScanQuery) QueryDnsentry() *DNSEntryQuery {
	query := (&DNSEntryClient{config: sq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := sq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := sq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(scan.Table, scan.FieldID, selector),
			sqlgraph.To(dnsentry.Table, dnsentry.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, scan.DnsentryTable, scan.DnsentryColumn),
		)
		fromU = sqlgraph.SetNeighbors(sq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryDomain chains the current query on the "domain" edge.
func (sq *ScanQuery) QueryDomain() *DomainQuery {
	query := (&DomainClient{config: sq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := sq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := sq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(scan.Table, scan.FieldID, selector),
			sqlgraph.To(domain.Table, domain.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, scan.DomainTable, scan.DomainColumn),
		)
		fromU = sqlgraph.SetNeighbors(sq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryPaths chains the current query on the "paths" edge.
func (sq *ScanQuery) QueryPaths() *PathQuery {
	query := (&PathClient{config: sq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := sq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := sq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(scan.Table, scan.FieldID, selector),
			sqlgraph.To(path.Table, path.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, scan.PathsTable, scan.PathsColumn),
		)
		fromU = sqlgraph.SetNeighbors(sq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryNameserver chains the current query on the "nameserver" edge.
func (sq *ScanQuery) QueryNameserver() *NameserverQuery {
	query := (&NameserverClient{config: sq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := sq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := sq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(scan.Table, scan.FieldID, selector),
			sqlgraph.To(nameserver.Table, nameserver.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, scan.NameserverTable, scan.NameserverColumn),
		)
		fromU = sqlgraph.SetNeighbors(sq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryRegistrar chains the current query on the "registrar" edge.
func (sq *ScanQuery) QueryRegistrar() *RegistrarQuery {
	query := (&RegistrarClient{config: sq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := sq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := sq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(scan.Table, scan.FieldID, selector),
			sqlgraph.To(registrar.Table, registrar.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, scan.RegistrarTable, scan.RegistrarColumn),
		)
		fromU = sqlgraph.SetNeighbors(sq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryWhois chains the current query on the "whois" edge.
func (sq *ScanQuery) QueryWhois() *WhoisQuery {
	query := (&WhoisClient{config: sq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := sq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := sq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(scan.Table, scan.FieldID, selector),
			sqlgraph.To(whois.Table, whois.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, scan.WhoisTable, scan.WhoisColumn),
		)
		fromU = sqlgraph.SetNeighbors(sq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Scan entity from the query.
// Returns a *NotFoundError when no Scan was found.
func (sq *ScanQuery) First(ctx context.Context) (*Scan, error) {
	nodes, err := sq.Limit(1).All(setContextOp(ctx, sq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{scan.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (sq *ScanQuery) FirstX(ctx context.Context) *Scan {
	node, err := sq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Scan ID from the query.
// Returns a *NotFoundError when no Scan ID was found.
func (sq *ScanQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = sq.Limit(1).IDs(setContextOp(ctx, sq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{scan.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (sq *ScanQuery) FirstIDX(ctx context.Context) int {
	id, err := sq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Scan entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Scan entity is found.
// Returns a *NotFoundError when no Scan entities are found.
func (sq *ScanQuery) Only(ctx context.Context) (*Scan, error) {
	nodes, err := sq.Limit(2).All(setContextOp(ctx, sq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{scan.Label}
	default:
		return nil, &NotSingularError{scan.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (sq *ScanQuery) OnlyX(ctx context.Context) *Scan {
	node, err := sq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Scan ID in the query.
// Returns a *NotSingularError when more than one Scan ID is found.
// Returns a *NotFoundError when no entities are found.
func (sq *ScanQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = sq.Limit(2).IDs(setContextOp(ctx, sq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{scan.Label}
	default:
		err = &NotSingularError{scan.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (sq *ScanQuery) OnlyIDX(ctx context.Context) int {
	id, err := sq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Scans.
func (sq *ScanQuery) All(ctx context.Context) ([]*Scan, error) {
	ctx = setContextOp(ctx, sq.ctx, ent.OpQueryAll)
	if err := sq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Scan, *ScanQuery]()
	return withInterceptors[[]*Scan](ctx, sq, qr, sq.inters)
}

// AllX is like All, but panics if an error occurs.
func (sq *ScanQuery) AllX(ctx context.Context) []*Scan {
	nodes, err := sq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Scan IDs.
func (sq *ScanQuery) IDs(ctx context.Context) (ids []int, err error) {
	if sq.ctx.Unique == nil && sq.path != nil {
		sq.Unique(true)
	}
	ctx = setContextOp(ctx, sq.ctx, ent.OpQueryIDs)
	if err = sq.Select(scan.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (sq *ScanQuery) IDsX(ctx context.Context) []int {
	ids, err := sq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (sq *ScanQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, sq.ctx, ent.OpQueryCount)
	if err := sq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, sq, querierCount[*ScanQuery](), sq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (sq *ScanQuery) CountX(ctx context.Context) int {
	count, err := sq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (sq *ScanQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, sq.ctx, ent.OpQueryExist)
	switch _, err := sq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("model_ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (sq *ScanQuery) ExistX(ctx context.Context) bool {
	exist, err := sq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ScanQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (sq *ScanQuery) Clone() *ScanQuery {
	if sq == nil {
		return nil
	}
	return &ScanQuery{
		config:         sq.config,
		ctx:            sq.ctx.Clone(),
		order:          append([]scan.OrderOption{}, sq.order...),
		inters:         append([]Interceptor{}, sq.inters...),
		predicates:     append([]predicate.Scan{}, sq.predicates...),
		withIpaddress:  sq.withIpaddress.Clone(),
		withAsninfo:    sq.withAsninfo.Clone(),
		withDnsentry:   sq.withDnsentry.Clone(),
		withDomain:     sq.withDomain.Clone(),
		withPaths:      sq.withPaths.Clone(),
		withNameserver: sq.withNameserver.Clone(),
		withRegistrar:  sq.withRegistrar.Clone(),
		withWhois:      sq.withWhois.Clone(),
		// clone intermediate query.
		sql:  sq.sql.Clone(),
		path: sq.path,
	}
}

// WithIpaddress tells the query-builder to eager-load the nodes that are connected to
// the "ipaddress" edge. The optional arguments are used to configure the query builder of the edge.
func (sq *ScanQuery) WithIpaddress(opts ...func(*IPAddressQuery)) *ScanQuery {
	query := (&IPAddressClient{config: sq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	sq.withIpaddress = query
	return sq
}

// WithAsninfo tells the query-builder to eager-load the nodes that are connected to
// the "asninfo" edge. The optional arguments are used to configure the query builder of the edge.
func (sq *ScanQuery) WithAsninfo(opts ...func(*ASNInfoQuery)) *ScanQuery {
	query := (&ASNInfoClient{config: sq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	sq.withAsninfo = query
	return sq
}

// WithDnsentry tells the query-builder to eager-load the nodes that are connected to
// the "dnsentry" edge. The optional arguments are used to configure the query builder of the edge.
func (sq *ScanQuery) WithDnsentry(opts ...func(*DNSEntryQuery)) *ScanQuery {
	query := (&DNSEntryClient{config: sq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	sq.withDnsentry = query
	return sq
}

// WithDomain tells the query-builder to eager-load the nodes that are connected to
// the "domain" edge. The optional arguments are used to configure the query builder of the edge.
func (sq *ScanQuery) WithDomain(opts ...func(*DomainQuery)) *ScanQuery {
	query := (&DomainClient{config: sq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	sq.withDomain = query
	return sq
}

// WithPaths tells the query-builder to eager-load the nodes that are connected to
// the "paths" edge. The optional arguments are used to configure the query builder of the edge.
func (sq *ScanQuery) WithPaths(opts ...func(*PathQuery)) *ScanQuery {
	query := (&PathClient{config: sq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	sq.withPaths = query
	return sq
}

// WithNameserver tells the query-builder to eager-load the nodes that are connected to
// the "nameserver" edge. The optional arguments are used to configure the query builder of the edge.
func (sq *ScanQuery) WithNameserver(opts ...func(*NameserverQuery)) *ScanQuery {
	query := (&NameserverClient{config: sq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	sq.withNameserver = query
	return sq
}

// WithRegistrar tells the query-builder to eager-load the nodes that are connected to
// the "registrar" edge. The optional arguments are used to configure the query builder of the edge.
func (sq *ScanQuery) WithRegistrar(opts ...func(*RegistrarQuery)) *ScanQuery {
	query := (&RegistrarClient{config: sq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	sq.withRegistrar = query
	return sq
}

// WithWhois tells the query-builder to eager-load the nodes that are connected to
// the "whois" edge. The optional arguments are used to configure the query builder of the edge.
func (sq *ScanQuery) WithWhois(opts ...func(*WhoisQuery)) *ScanQuery {
	query := (&WhoisClient{config: sq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	sq.withWhois = query
	return sq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Scanid string `json:"scanid,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Scan.Query().
//		GroupBy(scan.FieldScanid).
//		Aggregate(model_ent.Count()).
//		Scan(ctx, &v)
func (sq *ScanQuery) GroupBy(field string, fields ...string) *ScanGroupBy {
	sq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ScanGroupBy{build: sq}
	grbuild.flds = &sq.ctx.Fields
	grbuild.label = scan.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Scanid string `json:"scanid,omitempty"`
//	}
//
//	client.Scan.Query().
//		Select(scan.FieldScanid).
//		Scan(ctx, &v)
func (sq *ScanQuery) Select(fields ...string) *ScanSelect {
	sq.ctx.Fields = append(sq.ctx.Fields, fields...)
	sbuild := &ScanSelect{ScanQuery: sq}
	sbuild.label = scan.Label
	sbuild.flds, sbuild.scan = &sq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ScanSelect configured with the given aggregations.
func (sq *ScanQuery) Aggregate(fns ...AggregateFunc) *ScanSelect {
	return sq.Select().Aggregate(fns...)
}

func (sq *ScanQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range sq.inters {
		if inter == nil {
			return fmt.Errorf("model_ent: uninitialized interceptor (forgotten import model_ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, sq); err != nil {
				return err
			}
		}
	}
	for _, f := range sq.ctx.Fields {
		if !scan.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("model_ent: invalid field %q for query", f)}
		}
	}
	if sq.path != nil {
		prev, err := sq.path(ctx)
		if err != nil {
			return err
		}
		sq.sql = prev
	}
	return nil
}

func (sq *ScanQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Scan, error) {
	var (
		nodes       = []*Scan{}
		_spec       = sq.querySpec()
		loadedTypes = [8]bool{
			sq.withIpaddress != nil,
			sq.withAsninfo != nil,
			sq.withDnsentry != nil,
			sq.withDomain != nil,
			sq.withPaths != nil,
			sq.withNameserver != nil,
			sq.withRegistrar != nil,
			sq.withWhois != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Scan).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Scan{config: sq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, sq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := sq.withIpaddress; query != nil {
		if err := sq.loadIpaddress(ctx, query, nodes,
			func(n *Scan) { n.Edges.Ipaddress = []*IPAddress{} },
			func(n *Scan, e *IPAddress) { n.Edges.Ipaddress = append(n.Edges.Ipaddress, e) }); err != nil {
			return nil, err
		}
	}
	if query := sq.withAsninfo; query != nil {
		if err := sq.loadAsninfo(ctx, query, nodes,
			func(n *Scan) { n.Edges.Asninfo = []*ASNInfo{} },
			func(n *Scan, e *ASNInfo) { n.Edges.Asninfo = append(n.Edges.Asninfo, e) }); err != nil {
			return nil, err
		}
	}
	if query := sq.withDnsentry; query != nil {
		if err := sq.loadDnsentry(ctx, query, nodes,
			func(n *Scan) { n.Edges.Dnsentry = []*DNSEntry{} },
			func(n *Scan, e *DNSEntry) { n.Edges.Dnsentry = append(n.Edges.Dnsentry, e) }); err != nil {
			return nil, err
		}
	}
	if query := sq.withDomain; query != nil {
		if err := sq.loadDomain(ctx, query, nodes,
			func(n *Scan) { n.Edges.Domain = []*Domain{} },
			func(n *Scan, e *Domain) { n.Edges.Domain = append(n.Edges.Domain, e) }); err != nil {
			return nil, err
		}
	}
	if query := sq.withPaths; query != nil {
		if err := sq.loadPaths(ctx, query, nodes,
			func(n *Scan) { n.Edges.Paths = []*Path{} },
			func(n *Scan, e *Path) { n.Edges.Paths = append(n.Edges.Paths, e) }); err != nil {
			return nil, err
		}
	}
	if query := sq.withNameserver; query != nil {
		if err := sq.loadNameserver(ctx, query, nodes,
			func(n *Scan) { n.Edges.Nameserver = []*Nameserver{} },
			func(n *Scan, e *Nameserver) { n.Edges.Nameserver = append(n.Edges.Nameserver, e) }); err != nil {
			return nil, err
		}
	}
	if query := sq.withRegistrar; query != nil {
		if err := sq.loadRegistrar(ctx, query, nodes,
			func(n *Scan) { n.Edges.Registrar = []*Registrar{} },
			func(n *Scan, e *Registrar) { n.Edges.Registrar = append(n.Edges.Registrar, e) }); err != nil {
			return nil, err
		}
	}
	if query := sq.withWhois; query != nil {
		if err := sq.loadWhois(ctx, query, nodes,
			func(n *Scan) { n.Edges.Whois = []*Whois{} },
			func(n *Scan, e *Whois) { n.Edges.Whois = append(n.Edges.Whois, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (sq *ScanQuery) loadIpaddress(ctx context.Context, query *IPAddressQuery, nodes []*Scan, init func(*Scan), assign func(*Scan, *IPAddress)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Scan)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.IPAddress(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(scan.IpaddressColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.scan_ipaddress
		if fk == nil {
			return fmt.Errorf(`foreign-key "scan_ipaddress" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "scan_ipaddress" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (sq *ScanQuery) loadAsninfo(ctx context.Context, query *ASNInfoQuery, nodes []*Scan, init func(*Scan), assign func(*Scan, *ASNInfo)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Scan)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.ASNInfo(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(scan.AsninfoColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.scan_asninfo
		if fk == nil {
			return fmt.Errorf(`foreign-key "scan_asninfo" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "scan_asninfo" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (sq *ScanQuery) loadDnsentry(ctx context.Context, query *DNSEntryQuery, nodes []*Scan, init func(*Scan), assign func(*Scan, *DNSEntry)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Scan)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.DNSEntry(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(scan.DnsentryColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.scan_dnsentry
		if fk == nil {
			return fmt.Errorf(`foreign-key "scan_dnsentry" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "scan_dnsentry" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (sq *ScanQuery) loadDomain(ctx context.Context, query *DomainQuery, nodes []*Scan, init func(*Scan), assign func(*Scan, *Domain)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Scan)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.Domain(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(scan.DomainColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.scan_domain
		if fk == nil {
			return fmt.Errorf(`foreign-key "scan_domain" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "scan_domain" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (sq *ScanQuery) loadPaths(ctx context.Context, query *PathQuery, nodes []*Scan, init func(*Scan), assign func(*Scan, *Path)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Scan)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.Path(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(scan.PathsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.scan_paths
		if fk == nil {
			return fmt.Errorf(`foreign-key "scan_paths" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "scan_paths" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (sq *ScanQuery) loadNameserver(ctx context.Context, query *NameserverQuery, nodes []*Scan, init func(*Scan), assign func(*Scan, *Nameserver)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Scan)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.Nameserver(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(scan.NameserverColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.scan_nameserver
		if fk == nil {
			return fmt.Errorf(`foreign-key "scan_nameserver" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "scan_nameserver" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (sq *ScanQuery) loadRegistrar(ctx context.Context, query *RegistrarQuery, nodes []*Scan, init func(*Scan), assign func(*Scan, *Registrar)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Scan)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.Registrar(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(scan.RegistrarColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.scan_registrar
		if fk == nil {
			return fmt.Errorf(`foreign-key "scan_registrar" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "scan_registrar" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (sq *ScanQuery) loadWhois(ctx context.Context, query *WhoisQuery, nodes []*Scan, init func(*Scan), assign func(*Scan, *Whois)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Scan)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.Whois(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(scan.WhoisColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.scan_whois
		if fk == nil {
			return fmt.Errorf(`foreign-key "scan_whois" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "scan_whois" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (sq *ScanQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := sq.querySpec()
	_spec.Node.Columns = sq.ctx.Fields
	if len(sq.ctx.Fields) > 0 {
		_spec.Unique = sq.ctx.Unique != nil && *sq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, sq.driver, _spec)
}

func (sq *ScanQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(scan.Table, scan.Columns, sqlgraph.NewFieldSpec(scan.FieldID, field.TypeInt))
	_spec.From = sq.sql
	if unique := sq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if sq.path != nil {
		_spec.Unique = true
	}
	if fields := sq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, scan.FieldID)
		for i := range fields {
			if fields[i] != scan.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := sq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := sq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := sq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := sq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (sq *ScanQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(sq.driver.Dialect())
	t1 := builder.Table(scan.Table)
	columns := sq.ctx.Fields
	if len(columns) == 0 {
		columns = scan.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if sq.sql != nil {
		selector = sq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if sq.ctx.Unique != nil && *sq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range sq.predicates {
		p(selector)
	}
	for _, p := range sq.order {
		p(selector)
	}
	if offset := sq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := sq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ScanGroupBy is the group-by builder for Scan entities.
type ScanGroupBy struct {
	selector
	build *ScanQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (sgb *ScanGroupBy) Aggregate(fns ...AggregateFunc) *ScanGroupBy {
	sgb.fns = append(sgb.fns, fns...)
	return sgb
}

// Scan applies the selector query and scans the result into the given value.
func (sgb *ScanGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sgb.build.ctx, ent.OpQueryGroupBy)
	if err := sgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ScanQuery, *ScanGroupBy](ctx, sgb.build, sgb, sgb.build.inters, v)
}

func (sgb *ScanGroupBy) sqlScan(ctx context.Context, root *ScanQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(sgb.fns))
	for _, fn := range sgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*sgb.flds)+len(sgb.fns))
		for _, f := range *sgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*sgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ScanSelect is the builder for selecting fields of Scan entities.
type ScanSelect struct {
	*ScanQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ss *ScanSelect) Aggregate(fns ...AggregateFunc) *ScanSelect {
	ss.fns = append(ss.fns, fns...)
	return ss
}

// Scan applies the selector query and scans the result into the given value.
func (ss *ScanSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ss.ctx, ent.OpQuerySelect)
	if err := ss.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ScanQuery, *ScanSelect](ctx, ss.ScanQuery, ss, ss.inters, v)
}

func (ss *ScanSelect) sqlScan(ctx context.Context, root *ScanQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ss.fns))
	for _, fn := range ss.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ss.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
